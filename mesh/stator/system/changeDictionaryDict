FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      changeDictionaryDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

dictionaryReplacement
{
    boundary
    {
        AMI1
        {
            type            cyclicAMI;
            neighbourPatch  AMI2;
            transform       noOrdering;
        }
        AMI2
        {
            type            cyclicAMI;
            neighbourPatch  AMI1;
            transform       noOrdering;
        }
    }
}
